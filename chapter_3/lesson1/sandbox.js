//for loops

for (let i = 0; i < 5; i++){
    console.log('in loop:', i);
}
    console.log('loop finishd');

    const names = ['mustafa', 'musti', 'mustaa'];

    for( let k = 0; k < names.length; k++){
        //console.log( names[k]);
        let html = `<div>${names[k]}</div>`;
        console.log(html);
    }